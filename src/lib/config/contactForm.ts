// Shared between the contact form's client-side counter/maxlength and the server
// action's validation, so the two never drift out of sync.
export const MESSAGE_MIN_LENGTH = 10;
export const MESSAGE_MAX_LENGTH = 650;
